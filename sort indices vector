sort(indices.begin(), indices.end(), [&](int i, int j) {
            if (nums[i] == nums[j]) {
                return i < j; // If values are equal, pick the element with the smaller index
            }
            return nums[i] < nums[j];
        });

// way around --> // if there are duplicate elements in array

stable_sort ...


// must hard code it for it to be stable or use stable sort stl
// that & allows to access global values
