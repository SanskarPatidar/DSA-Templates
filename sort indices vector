sort(indices.begin(), indices.end(), [&inputNums](int i, int j) {
            return inputNums[i] < inputNums[j];  // Compare values in inputNums at indices
        });

NOTE --> don't use <= in comp since it can result unintended result, so for stability, just use <
