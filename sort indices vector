sort(indices.begin(), indices.end(), [&inputNums](int i, int j) {
            if (nums[i] == nums[j]) {
                return i < j; // If values are equal, pick the element with the smaller index
            }
            return nums[i] < nums[j];
        });


NOTE --> must hard code it for it to be stable or use stable sort stl
